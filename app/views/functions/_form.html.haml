- semantic_form_for @function do |f|
  = f.error_messages
  - f.inputs do
    = f.input :name
    = f.input :description
    = f.input :state, :as => :radio, :label => 'Active', :collection => [['Yes', 'active'], ['No', 'inactive']]
  = f.buttons